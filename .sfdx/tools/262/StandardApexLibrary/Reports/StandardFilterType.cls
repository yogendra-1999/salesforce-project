/**
 * The StandardFilterType enum describes the type of standard filters in a report. The getType() method returns a Reports.StandardFilterType enum value.
 */
global enum StandardFilterType {
  PICKLIST,
  STRING
}