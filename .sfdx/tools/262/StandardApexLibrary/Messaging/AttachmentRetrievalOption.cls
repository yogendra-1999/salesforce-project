/**
 * Provides options for including attachment metadata only, attachment metadata and content, or excluding attachments.
 */
global enum AttachmentRetrievalOption {
  METADATA_ONLY,
  METADATA_WITH_BODY,
  NONE
}