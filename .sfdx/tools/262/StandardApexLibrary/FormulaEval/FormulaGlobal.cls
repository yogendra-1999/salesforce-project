/**
 * Specifies a global variable that references data in your organization in the withGlobalVariables(formulaGlobals) method.
 */
global enum FormulaGlobal {
  CUSTOMMETADATA,
  LABEL,
  ORGANIZATION,
  PERMISSION,
  PROFILE,
  SETUP,
  SYSTEM,
  USER,
  USERROLE
}