/**
 * Specifies the return type for the withReturnType(returnType) method.
 */
global enum FormulaReturnType {
  BOOLEAN,
  DATE,
  DATETIME,
  DECIMAL,
  DOUBLE,
  ID,
  INTEGER,
  LONG,
  STRING,
  TIME
}